# Work Firewall — Python dependencies

# Core
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.0
openai>=1.30.0

# MCP server
mcp>=1.0.0

# (Bifrost is started separately via: npx -y @maximhq/bifrost)
